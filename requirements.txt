# Core
openmeteo-requests>=1.0.0
pandas>=2.0.0
requests-cache>=1.0.0
retry-requests>=2.0.0
python-dotenv>=1.0.0

# Hopsworks (with all extras to avoid missing dependencies)
hopsworks[python]==4.7.*

# Optional but recommended
pyarrow>=15.0.0